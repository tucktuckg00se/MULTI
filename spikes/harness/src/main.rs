//! `latency`: measures video pass-through delay and caption lag for M0 spikes.
//! Placeholder; implemented by the harness agent (see docs/m0/README.md).

fn main() -> anyhow::Result<()> {
    anyhow::bail!("latency tool not implemented yet")
}
